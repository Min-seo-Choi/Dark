using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventButtonActive : MonoBehaviour
{
    /* 게임진행도 체크해서 이벤트 다시보기 버튼 활성화 */
    [SerializeField]
    UISprite[] eventSprite;   

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
